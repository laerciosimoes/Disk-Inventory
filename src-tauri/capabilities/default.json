{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main picker window and dynamically-created volume windows",
  "windows": ["main", "volume-*"],
  "permissions": [
    "core:default",
    "core:menu:default",
    "opener:default"
  ]
}
